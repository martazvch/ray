globalSet: fn() -> void
globalGet: fn() -> void

fn set_vars() {
    a := "init"

    // Here, assignment refers to heap variable
    fn set() { a = "updated" }
    fn get() { print(a) }

    globalSet = set
    globalGet = get
}

fn main() {
    set_vars()

    globalSet()
    globalGet()
}

