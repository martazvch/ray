struct Foo { x: int }

fn f(a: *Foo) {
    a.x = 8
}

fn main() {
    let a = 1
    let ptr = *a
    print ptr.*

    ptr.* = 6
    print ptr.*

    let foo = Foo{ x: 4 }
    print foo.x
    f(*foo)
    print foo.x
}

