struct Foo { x: int }

fn f(a: *Foo) {
    a.x = 8
}

fn main() {
    a := 1
    ptr := *a
    print(ptr.*)

    ptr.* = 6
    print(ptr.*)

    foo := Foo{ x=4 }
    print(foo.x)
    f(*foo)
    print(foo.x)
}

