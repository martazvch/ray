enum Marker {
    cross,
    circle,
    none

    fn init() -> Self {
        return Self.cross
    }

    fn toString(self) -> str {
        if self == Self.cross do return "x"
        if self == Self.circle do return "o"
        return "x"
    }
}

struct Cell {
    marker: Marker = Marker.cross,
    marker2 = Marker.circle
}

fn markerStuff(m: Marker = Marker.circle) {
    _ = m.toString()
}

fn markerStuff2(m = Marker.cross) {
    _ = m.toString()
}

fn main() {
    cell := Cell{}
    _ = cell.marker
    _ = cell.marker.toString()
    _ = cell.marker2
    _ = cell.marker2.toString()

    markerStuff()
    markerStuff2()
}


