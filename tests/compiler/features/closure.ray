globalSet: fn() -> void
globalGet: fn() -> str

fn set_vars() {
    a := "init"

    // Here, assignment refers to heap variable
    fn set() { a = "updated" }
    fn get() -> str { return a }

    globalSet = set
    globalGet = get
}

fn main() {
    set_vars()

    globalSet()
    globalGet()
}
