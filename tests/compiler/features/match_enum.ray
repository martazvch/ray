enum Marker {
    cross,
    circle,
    none
}

enum Bar{a}

fn main() {
    marker: Marker = .none
    bar: Bar = .a

    match marker {
        Marker.circle => _ = "circle"
        Marker.cross => _ = "cross"
        Marker.none => _ = "none"
    }

    match marker {
        .circle => _ = "circle"
        .cross => _ = "cross"
        .none => _ = "none"
    }

    foo := match marker {
        .circle => 1
        .cross => 2
        .none => {
            tmp := 4
            tmp2 := 6
            break 3
        }
    }

    match marker {
        .circle => _ = "circle"
        .cross => _ = "cross"
        else => "invalid"
    }
}

