trait Foo {
    fn speak(self, a: int)
}

struct Bar {
    impl Foo {
        fn speak(self, a: int) {
            print "Bar " + str(a)
        }
    }
}

struct Baz {
    impl Foo {
        fn speak(self, a: int) {
            print "Baz " + str(a)
        }
    }
}

fn f(value: Foo) {
    value.speak(2)
}

fn main() {
    let bar = Bar{}
    f(bar)
    let baz = Baz{}
    f(baz)
}

