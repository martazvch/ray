enum Foo {
    a,
    b,
    c,
}

fn main() {
    f: Foo|int = Foo.a

    match f is {
        Foo {
            .a => _ = "a"
            .b => _ = "b"
            .c => _ = "c"
        }
        int => _ = "int"
    }
}

