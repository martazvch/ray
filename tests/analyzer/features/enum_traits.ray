trait Bar {
    fn speak(self)
}
enum Foo {
    a,
    b,

    impl Bar {
        fn speak(self) {
            self == .a ? "yes" : "no"
        }
    }
}

fn main() {
    foo: Foo = .b
    foo.speak()

    bar: Bar = foo
    bar.speak()
}

