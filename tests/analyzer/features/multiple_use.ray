use data.math
use data.geom

fn main() {
    middle := geom.Point { x=1, y=90 }
    middle.display()
    
    square := geom.Square { side=10, middle }
    square.middle.x
    square.middle.y

    math.introduce()
    vec := math.getVecFromPos(square.middle.x, square.middle.y)
    vec.x
    vec.y

    {
        use data.sub.simple_str
        simple_str.quote("local import")
        middle.display()
        simple_str.concat("just ", "concaneted")
    }

    new_middle := square.moveCenter(middle)
    new_middle.display()
    square.middle.x
    square.middle.y
}

