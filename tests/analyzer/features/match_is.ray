fn main() {
    a: int|float = 8.3

    match a is {
        int => a + 1
        float => "float"
    }

    b := match a is {
        int => 1
        else => 5
    }

    trash := true
    match a is {
        float {
            1.2 => "first"
            4.5..10.0 => "second"
            else => "non"
        }
        int => "int"
    }
}

