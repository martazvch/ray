fn main() {
    // Only one path produces a value
    a := if true { return } else 4
    b := if false do 5 else return

    fn func(a: int) -> int {
        // Only one path generates a value, returns a value otherwise
        if true do "str" else return a

        tmp: str = if true do return a + 2 else {
            local := 56.4
            break "champ"
        }

        // If without else but not as last stmt
        if true do return 8

        return if true {
            if true {
                return { break 1 }
            } else break 5
        } else {
            if false {
                return if true { return 4 } else 3
            } else break 8
        }
    }
}
