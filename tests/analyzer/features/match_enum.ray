enum Marker {
    cross,
    circle,
    none
}

enum Bar{a}

fn main() {
    marker: Marker = .none
    bar: Bar = .a

    match marker {
        Marker.circle => "circle"
        Marker.cross => "cross"
        Marker.none => "none"
    }

    match marker {
        .circle => "circle"
        .cross => "cross"
        .none => "none"
    }

    foo := match marker {
        .circle => 1
        .cross => 2
        .none => {
            tmp := 4
            tmp2 := 6
            break 3
        }
    }

    match marker {
        .circle => {}
        else => {}
    }
}

