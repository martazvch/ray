union Foo { a, b }
union Bar {
    a: int,
    b: str,
}

fn main() {
    foo: Foo = .a
    if foo == .a do "I'm foo.a"
    else if foo == .b do "I'm foo.b"

    bar: Bar = .b("ee")
    if bar == Bar.a do "I'm bar.a"
    else if bar == .b do "I'm bar.b"
}

