enum Foo {
    a,
    b,
    c,
}

fn main() {
    f: Foo|int = Foo.a

    match f is {
        Foo {
            .a => "a"
            .b => "b"
            .c => "c"
        }
        int => "int"
    }
}
