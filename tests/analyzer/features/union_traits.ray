trait Foo {
    fn speak() { "oui" }
    fn speakNoDef()

    fn method(self) { "method" }
    fn methodNoDef(self)
}

union Bar {
    a: int,
    b: str,
    
    impl Foo {
        fn speakNoDef() { "noDef" }
        fn methodNoDef(self) {
            if self == .a {
                "I'm a: " + str(self.a)
            } else {
                "I'm b: " + self.b
            }
        }
    }
}

fn main() {
    Bar.speak()
    Bar.speakNoDef()

    bar: Bar = .a(1)
    bar.method()
    bar.methodNoDef()
}
