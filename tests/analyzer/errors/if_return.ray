fn main() {
    // First error, retuns an int
    a := if true { return 1 } else 4
    // Second, same with str and no block
    b := if false do 5 else return "oui"

    // If without else in non-partial context
    c := if true do 6

    fn func(a: int, b: float) -> int {
        // Returns b, which is a float
        _ =  if true do "str" else return b

        tmp: str = if true do return a + 2 else {
            local := 56.4
            if false do break "champ"
        }

        // Raises no error
        return if true {
            if true {
                return { break 1 }
            } else break 5
        } else {
            if false {
                return if true { return 4 } else 3
            } else break 8
        }
    }

    fn func2() -> int {
        return if true {
            if true {
                return 1
            } else break 5
        } else {
            if false {
                return if true { return 4 } else 3.6
            } else break 8
        }
    }
}
