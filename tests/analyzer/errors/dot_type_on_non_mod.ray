struct Vec2 {
    x: int, y: int,

    fn new(x: int, y: int) -> Self {
        return Self { x, y }
    }
}

fn main() {
    var v = Vec2.new(12, 99)

    fn invalid(a: v.x) {}
    fn invalid2(a: Vec2.new) {}
}
