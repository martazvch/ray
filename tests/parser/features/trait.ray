trait Foo {
    fn eq(self, other: int) -> float
    fn hash(self, other: int) {
        print self
        print other
    }
}

struct Foo {
    x, y: int

    impl Bar {}
    impl Baz {
        fn eq(self, other: str) -> bool {
            return true
        }
        fn hash() {}
    }

    fn ownFn(self, a: float) {}
}

union Foo {
    x, y,

    impl Bar {}
    impl Baz {
        fn eq(self, other: str) -> bool {
            return true
        }
        fn hash() {}
    }

    fn ownFn(self, a: float) {}
}
